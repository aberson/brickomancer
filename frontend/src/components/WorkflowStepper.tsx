import React from "react";

/** Top-level step manager (Steps 1–4). Implemented in Step 10. */
function WorkflowStepper(): React.ReactElement {
  return <div>WorkflowStepper — coming in Step 10</div>;
}

export default WorkflowStepper;
